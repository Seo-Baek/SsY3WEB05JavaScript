객체
	- 자바스크립트의 객체는 크게 내장객체, 
	     브라우저 객체 모델(BOM : Browser Object Model),
	     문서 객체(DOM : Document Object Model)로 나누어 진다.
	    
	    1. 내장객체
	    	- 자바스크립트 엔진에 내장되어 있으며, 필요한 경우 생성해서 사용할 수 있다.
	    	- 내장 객체로는 문자(String), 날짜(Date), 배열(Array), 수학(Math) 등이 있다.
		2. 브라우저 객체 모델(BOM : Browser Object Model)
			- 브라우저에 계층적으로 내장되어있는 객체들을 말한다.
			- 브라우저 객체 모델에는 
			  window, screen, location, history, navigation 객체 등이 있음.
		3. 문서 객체 모델(DOM : Document Object Model)
			- HTML 문서 구조를 말함.
			- 예를 든다면 <HTML>이 있으며, 그 하위에는 <head>와 <body>
			  <head> 하위에는 <meta>와 <title>이 있음.
			- 구조를 따라가기 때문에 계층적이다.(Tree)
			- Root(여기서는 HTML)의 자손은 Depth가 2 이상이면 전부 포함되나 
			    자식은 Depth가 1인 Node뿐임.
			- 자바스크립트의 문서 객체 모델은 IE 8 이하에서는 사용이 힘들다는 단점이 있음.